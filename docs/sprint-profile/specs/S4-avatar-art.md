# S4 — Default profile avatar (painterly art)

**Goal:** a default profile picture in the Harvest painterly golden-hour style, used as the avatar
placeholder (founder comment). **Done** in Phase 6-early.

- Generated via the nano-banana-2 MCP: a faceless gender-neutral bust, oil/gouache golden-hour palette,
  on a `#F1E6D2` cream background (blends onto `bg-cream`).
- Saved `assets/default-avatar.png`, 256×256, ~94KB.
- Wired as the avatar image in the recipes header (S2) and the profile identity block (S2).

**AC:** avatar renders in the header + profile screen, circular-clipped, on-theme. → sim demo.
