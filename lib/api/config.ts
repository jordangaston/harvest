// The Harvest backend. iOS simulator reaches the host over localhost (app.json
// allows local-network cleartext). Change here if the server moves.
export const API_BASE_URL = "http://localhost:3000";
