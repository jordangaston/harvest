{
  "name": "@harvest/spike-cf",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SPIKE — Cloudflare-serverless target for the Harvest backend (Workers + Workflows + D1). Not production.",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:apply-local": "node scripts/apply-schema.mjs",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "proof": "bash scripts/proof.sh"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4",
    "drizzle-orm": "^0.44.6",
    "hono": "^4.6.14",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "drizzle-kit": "^0.31.6",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.123.0"
  }
}
